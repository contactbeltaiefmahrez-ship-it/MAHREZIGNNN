# Demo mode needs no database. Leave unset for auto-detect:
# the app tries the API and falls back to labelled demo data.
# NEXT_PUBLIC_DATA_MODE=demo
# NEXT_PUBLIC_API_URL=http://localhost:4000
# NEXT_PUBLIC_PMTILES_URL=  # unset = honest degraded map
